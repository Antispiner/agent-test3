# Ancestor Chat

Talk to your ancestors. Spring + React, dark UI matching nerw-ecosystem.

## Run

`make demo` (filled in by devops phase)

## Architecture

nginx → Spring Boot → Anthropic Messages API. SQLite for profiles + history.
